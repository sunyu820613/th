# 第 13 章　从 Servlet 到 MVC

## 本章目标
理解纯 Servlet 写法在项目变大之后会遇到的具体痛点；明白为什么需要一个"统一入口"来分发请求，为什么需要把"业务逻辑"和"页面展示"分开；为后面学习 MVC 模式做铺垫。

## 一句话理解
一个 Servlet 处理一个 URL，页面一多，你就要写一堆结构几乎一样、只是内容不同的类——这种"重复"本身，就是在提醒你：应该有更好的组织方式。

## 为什么需要它
上一章结尾留了一个思考题：如果网站有 50 个页面，要写多少个 Servlet？答案是至少 50 个类，而且每个类内部还会重复大量相似的代码。这一章我们把这个问题摊开讲清楚，不是为了批评 Servlet 写法"不好"（它在历史上、在理解原理上都非常有价值），而是为了让你亲身体会"为什么后面要学 MVC、要学 Spring MVC"——任何一个新技术的出现，都是为了解决某个具体的痛点，而不是"因为它更时髦"。

## 核心概念

我们先假设一个具体场景：一个小型电商网站，需要下面这些页面/接口：

- 查看用户列表
- 查看用户详情
- 新增用户
- 修改用户
- 删除用户
- 查看商品列表
- 查看商品详情
- ……（商品、订单、购物车，每个业务对象都要重复一遍类似的增删改查）

按纯 Servlet 的写法，你大概会写出这样的结构：

```
UserListServlet      @WebServlet("/users")
UserDetailServlet     @WebServlet("/users/detail")
UserAddServlet        @WebServlet("/users/add")
UserUpdateServlet     @WebServlet("/users/update")
UserDeleteServlet     @WebServlet("/users/delete")
ProductListServlet    @WebServlet("/products")
ProductDetailServlet  @WebServlet("/products/detail")
... 以此类推
```

哪怕只做"用户"和"商品"两个业务对象的基础增删改查，就已经写了 10 个类。这就是**第一个痛点：类的数量随着页面数量线性增长，写不完，也记不住**。

## 图解

把其中一个 Servlet 的内部结构画出来，看看每个类里实际都在干什么：

```
UserAddServlet.doPost(req, resp)
  │
  ├─① 从 req 里手动取出参数
  │     String name = req.getParameter("name");
  │     String email = req.getParameter("email");
  │
  ├─② 手动校验参数
  │     if (name == null || name.isEmpty()) { ...返回错误提示... }
  │     if (!email.contains("@")) { ...返回错误提示... }
  │
  ├─③ 手动写业务逻辑（比如保存到某个 List 或数据库）
  │     userList.add(new User(name, email));
  │
  └─④ 手动拼 HTML 字符串，写回响应
        resp.getWriter().println("<html>...新增成功...</html>");
```

现在想象一下：`UserUpdateServlet`、`ProductAddServlet` 里的 ①②④ 这三步，代码结构几乎一模一样，只是取的字段名、校验规则、拼的 HTML 内容不同。**第二个痛点**由此浮现：**"取参数"、"校验参数"、"拼页面"这些通用逻辑，在每个 Servlet 里都被重复写了一遍**。

再往深处看，还有**第三个痛点**：一个 Servlet 类里，同时塞进了"业务逻辑怎么处理数据"（比如怎么保存用户）和"页面长什么样"（拼 HTML 字符串）这两件完全不同的事情。这两件事变化的原因不一样——业务逻辑可能因为需求调整而改，页面样式可能因为美工改版而改——但纯 Servlet 写法把它们硬揉在同一个方法里，谁都不敢轻易动这个类，改一点点都可能牵连另一部分。

## 最小示例

用一段更接近真实的伪代码，展示"职责混乱"具体长什么样（这段代码只是用来说明问题，不要求能直接编译运行）：

```java
@WebServlet("/users/add")
public class UserAddServlet extends HttpServlet {

    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp)
            throws IOException {

        // 第 1 件事：从请求里取参数（重复劳动）
        String name = req.getParameter("name");
        String email = req.getParameter("email");

        // 第 2 件事：手动校验（重复劳动，还容易漏校验）
        if (name == null || name.trim().isEmpty()) {
            resp.getWriter().println("<p>姓名不能为空</p>");
            return;
        }

        // 第 3 件事：业务逻辑（这才是这个 Servlet 真正独有的部分）
        User user = new User(name, email);
        userRepository.save(user);

        // 第 4 件事：手动拼 HTML（和业务逻辑混在一起，页面一改就要动这个类）
        resp.setContentType("text/html;charset=UTF-8");
        resp.getWriter().println("<html><body>");
        resp.getWriter().println("<h1>新增成功：" + name + "</h1>");
        resp.getWriter().println("</body></html>");
    }
}
```

## 代码逐行解释

对照上面这段伪代码，逐段说明问题出在哪里：

- 第 1 件事（取参数）：`req.getParameter(...)` 这种写法，在每一个需要接收表单/参数的 Servlet 里都会重复出现，而且字段一多，容易漏取或者拼错参数名，编译器也帮不了你——因为 `getParameter` 返回的永远是 `String`，取错了名字只会在运行时才发现拿到 `null`。
- 第 2 件事（校验）：校验逻辑散落在每个 Servlet 内部，没有统一的地方管理，容易有的 Servlet 校验严格、有的 Servlet 忘了校验，规则不一致。
- 第 3 件事（业务逻辑）：这才是这个类真正应该关心的核心内容——"新增一个用户"这件事本身。
- 第 4 件事（拼 HTML）：把页面展示的细节（标签、样式、文字）和业务逻辑写在同一个方法体里，业务逻辑的开发者和页面样式的调整者互相干扰，谁都不敢单独改这个类而不影响另一半。

这四件事混在一起，正是"职责不清"的具体表现：**一个方法承担了太多不相关的职责**。

## 程序运行过程

这一章重点不是跑一段代码，而是理清"问题是怎么随着项目变大而暴露出来的"这条逻辑链：

1. 项目刚开始只有 1、2 个页面时，每个页面写一个 Servlet，代码量不大，问题不明显。
2. 页面涨到 10 个、20 个，你发现自己在不断复制粘贴"取参数 → 校验 → 拼 HTML"这几段代码，只是改改字段名。
3. 某天产品经理要求"所有页面的错误提示样式统一改一下"，你发现要去几十个 Servlet 里逐个修改拼 HTML 的那几行代码，工作量巨大且容易漏改。
4. 某天需要给"新增用户"加一条新的校验规则，你发现business逻辑和页面拼接代码写在一起，改动业务逻辑时不小心可能牵连到页面输出的格式。
5. 于是自然会想：**能不能有一个统一的入口，先把"取参数、校验"这类通用工作做掉，再把真正的业务逻辑单独抽出来管理，页面展示也单独交给专门的部分处理？**

这个"统一的入口"，后面学 Spring MVC 时你会看到框架已经把它写好了，就是 **DispatcherServlet**——这里先理解"为什么需要它"，具体它是怎么实现的，留到第 20 章详细讲。

## 常见错误

| 现象/误区 | 原因 | 更好的思路 |
|---|---|---|
| 认为"多写几个 Servlet 类没什么大不了" | 只在页面很少的小项目里体会不到痛点 | 想象项目要维护 3 年、页面涨到 200 个，重复劳动和维护成本会呈数量级增长 |
| 把业务逻辑和页面拼接写在同一个方法里 | 图省事，一开始项目小看不出问题 | 从一开始就有意识地把"处理数据的逻辑"和"生成页面内容"分开写、分开放 |
| 以为"统一入口"意味着所有请求都执行同一段代码 | 对"统一分发"理解有误 | 统一入口指的是所有请求都先经过同一个地方，由它根据请求的不同去调用不同的具体处理逻辑，而不是所有请求都执行一模一样的代码 |

## 动手练习

1. 假设你要给"用户模块"和"商品模块"各加一个"批量导入"的页面，按纯 Servlet 写法，你大概要新增几个类？把类名列出来。
2. 回顾第 12 章你写的 `HelloServlet`，如果要在里面加上"记录这次访问的日志"这个功能，你会把这段日志代码写在哪里？如果有 20 个 Servlet 都需要记日志，你会怎么做（提示：回忆第 11 章讲过的 Filter）？
3. 用自己的话说一说："业务逻辑"和"页面展示"分开管理，具体能带来什么好处？

## 小测验

1. 纯 Servlet 写法在项目变大后，主要暴露出哪几类问题？
2. "统一分发入口"大致要解决什么问题？
3. 后面 Spring MVC 里负责"统一分发"的那个组件叫什么名字（本章只需要记住名字，具体实现原理留到后面）？

<details>
<summary>参考答案</summary>

1. 主要是：类的数量随页面数量线性增长；取参数、校验、拼页面这类通用代码在多个 Servlet 里重复出现；业务逻辑和页面展示混在同一个方法里，职责不清、维护困难。
2. 统一分发入口要解决的是：所有请求先经过同一个地方，由它统一处理一些通用工作（比如找到该由谁处理这个请求），再把真正的业务处理转交出去，避免每个处理单元各自为政、重复劳动。
3. `DispatcherServlet`，具体实现原理会在第 20 章详细讲解。
</details>

## 本章总结
你已经理解了纯 Servlet 写法在项目变大后会遇到的具体问题：类爆炸、重复代码、职责混乱，也明白了为什么需要一个统一的分发入口、需要把业务逻辑和页面展示分开。下一章我们正式学习 MVC 模式——先讲清楚它经典的三要素，再讲现代 Spring Boot 项目里更细致的分层方式。
